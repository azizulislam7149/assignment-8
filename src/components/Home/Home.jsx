import React from 'react';
import Banner from '../Banner/Banner';
import Camera from '../Camera/Camera';

const Home = () => {
    return (
        <div>
            <Banner></Banner>
            <Camera></Camera>
        </div>
    );
};

export default Home;